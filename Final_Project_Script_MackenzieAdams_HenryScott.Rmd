---
output: html_document
editor_options: 
  chunk_output_type: console
chunk_output_type: console
---
Mackenzie Adams and Henry Scott

```{r, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

```

Install and load needed packages

```{r}
library(dataRetrieval)
library(tidyverse)
library(dplyr)
library(patchwork)
library(zoo)
library(daymetr) 
library(leaflet) 
library(fs)
library(ggpubr) # for adding R and P vals to graphs
library(DescTools) # for doing Mode statistical calc
library(rstatix)
library(midasr)
library(ggiraphExtra)
```

```{r}
# USGS GAGE INFO/DATA 

# This efficiently automates the process of getting all the stream data desired into one df.

gage_ids <- 
  tribble(
    ~site_name,        ~site_no,
    "Dungeness_River",  "12048000",
    "Elwha_River",      "12045500",
    "Skokomish_River",  "12061500")

# set all the parameters
start <- date("1982-10-01")
end <- date("2023-9-30")
para <-c("00060", "00065")

# if sites in gage_ids are changed, uncomment the code below and run it once. This stores the data locally and eventually in the repo if its pushed. This is mostly to be polite to the USGS hosting service.

# NWIS_data <- readNWISdv(gage_ids$site_no, para, start, end)
# saveRDS(NWIS_data, "NWIS_data.rdata")
# 
# NWIS_meta <- readNWISsite(gage_ids$site_no)
# saveRDS(NWIS_meta, "NWIS_meta.rdata")

gage_ids <- 
  readRDS("NWIS_meta.rdata") %>% 
  select(site_no,
         dec_lat_va, 
         dec_long_va) %>%
  inner_join(gage_ids, .)

gage_data <-
  readRDS("NWIS_data.rdata") %>%
  inner_join(gage_ids, .) %>%
  renameNWISColumns() %>%
  drop_na(Flow) %>%
  mutate(water_year = calcWaterYear(Date)) %>%
  group_by(site_name, site_no)


# Shows all unique grouped elements in gage_data. Verifies that all the desired gages were selected and are in the data frame.

summarize(gage_data)

```


```{r}
# WEATHER DATA ACQUISITION: This code retrieves the desired precipitation and temperature data.

# write_csv(x = select(gage_ids, -"site_no"), file = "gage_ids.csv")
# 
# daymet_all <- download_daymet_batch(file_location = "./gage_ids.csv", start = year(start), end = year(end), internal = T, simplify = F)
# saveRDS(daymet_all, "DAYMET_all.rdata")


# Downloading data works. Now to append it...

daymet_reorg <-
  function(f) {
    bind_cols(data = f$data, site = f$site, .name_repair = "unique")
  } 

gage_data_all <- 
  lapply(readRDS("./DAYMET_all.rdata"), daymet_reorg) %>%
  bind_rows() %>%
  rename(doy = yday, 
         ann = year,
         prcp_daily_mm = prcp..mm.day.,
         t_max_C = tmax..deg.c.,
         t_min_C = tmin..deg.c.) %>%
  mutate(
    Date = make_datetime(year = ann, day = doy),
    t_avg_C = (t_max_C + t_min_C)/2) %>%
  select(site,
         Date,
         prcp_daily_mm,
         t_max_C,
         t_min_C,
         t_avg_C,
         ann
  ) %>%
  left_join(ungroup(gage_data), ., join_by(site_name == site, Date == Date)) %>%
  drop_na(t_min_C)

#Now the discharge, precipitation, and temperature data is all in one data frame.
```

#DO WE NEED ALL OF THIS CODE? IS SOME OF IT A REPEAT OF MY NEW OFFSET DATA CODE?
```{r}
# ADD FISH DATA: This code reads in csv files containing salmon return data downloaded for the particular rivers of interest from the stateofsalmon.wa.gov site.

fish_files <-
  dir_ls("./Salmon_Data/")


# creates a df of fish data. Originally, the code 1 chunk below this did this within its own workflow but I made it a seperate df bc i need it somewhere else, not attached to climate data
fish_data <-
  fish_files %>%
  lapply(read_csv) %>%
  bind_rows() %>%
  filter(data_type == "TSAEJ") %>%
  
  # this line moves the allignment of the fish data by a year bc supposedly that makes things line up better. Its like 11:30 and im tired and i dont trust myself to solve dates
  mutate(site_name = paste0(str_extract(population_name, "^\\w+"), "_River"), .before = "stock_number", year = year + 1) %>% 
  select(abundance_qty,
         year,
         site_name) %>%
  drop_na(abundance_qty)

gage_fish_all <-
  fish_files %>%
  lapply(read_csv) %>%
  bind_rows() %>%
  filter(data_type == "TSAEJ") %>%
  mutate(site_name = paste0(str_extract(population_name, "^\\w+"), "_River"), .before = "stock_number") %>% 
  select(abundance_qty,
         year,
         site_name) %>%
  drop_na(abundance_qty) %>%
  left_join(gage_data_all,., by = c(
    "water_year" = "year",
    "site_name" = "site_name"
  )
  )

# Make an offset df so that returns line up with the climate year they spawned in
gage_fish_lead <-
  gage_fish_all %>%
  group_by(site_name) %>% 
  mutate(abundance_lead = lead(abundance_qty, 365*4),
         Date_lead = Date + years(4),
         ann_lead = year(Date_lead)
         )
```


```{r}
#Offsetting the climate data by four years (ex. salmon returns that occurred during 1990's water year are now dependent on 1986 climate data) and assigning the salmon return values to change at each new water year.

gage_fish_lead_2 <- gage_fish_all %>%
  mutate(Date = as.Date(Date))
  

env_data <- gage_fish_all %>%
 filter(year(Date) >= 1982 & year(Date) <= 2023) %>%
 select(site_name, Date, prcp_daily_mm, Flow, t_max_C, t_min_C, t_avg_C)

env_data <- env_data %>% 
  mutate(Date = Date + years (4))

abundance_data <- gage_data_all %>%
  filter(year(Date) >= 1986 & year(Date) <= 2023) %>%
  select(site_name, Date) 


aligned_data <- 
  abundance_data %>%
  left_join(env_data, by = c("site_name", "Date")) %>%
  addWaterYear() %>%
  left_join(., fish_data, by = c(
    "site_name" = "site_name",
    "waterYear" = "year"))

```


```{r}
#This code produces a figure that shows years on the x axis and discharge on the y-axis for the three rivers of interest. The Dungeness River produces substantially less discharge compared to the Elwha and the Skokomish, therefore we set the boundaries for all three to be "free" so that patterns in discharge can more easily be examined and the data fills up the whole graph area.

q_gg <-
  aligned_data_clean %>%
  ggplot(aes(x = Date, y = Flow, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Discharge (cfs)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none", )

#Shows river discharge over time for the Skokomish.
q_gg_Skokomish <-
  aligned_data_clean %>%
  filter(site_name == "Skokomish River") %>%
  ggplot(aes(x = Date, y = Flow, color = site_name)) +
  geom_line(color = "dodgerblue") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Discharge (cfs)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none", )

#Shows river discharge over time for the Dungeness.
q_gg_Dungeness <-
  aligned_data_clean %>%
  filter(site_name == "Dungeness River") %>%
  ggplot(aes(x = Date, y = Flow, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Discharge (cfs)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none", )

#Shows river discharge over time for the Elwha.
q_gg_Elwha <-
  aligned_data_clean %>%
  filter(site_name == "Elwha River") %>%
  ggplot(aes(x = Date, y = Flow, color = site_name)) +
  geom_line(color = "limegreen") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Discharge (cfs)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none", )
```


```{r}
#This code generates a plot with year on the x-axis and precipitation(mm) on the y-axis for the areas where the three streams of interest are located.

prcp_gg <-
  aligned_data_clean %>%
  ggplot(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Precipitation (mm)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Elwha. 
prcp_gg_Elwha <-
  aligned_data_clean %>%
  filter(site_name == "Elwha River") %>%
  ggplot(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  geom_line(color = "limegreen") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Precipitation (mm)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Dungeness.
prcp_gg_Dungeness <-
  aligned_data_clean %>%
  filter(site_name == "Dungeness River") %>%
  ggplot(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Precipitation (mm)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Skokomish. 
prcp_gg_Skokomish <-
  aligned_data_clean %>%
  filter(site_name == "Skokomish River") %>%
  ggplot(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  geom_line(color = "dodgerblue") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Precipitation (mm)", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

```


```{r}
#This code generates a plot with year on the x-axis and average temperature(C) on the y-axis for the areas where the three streams of interest are located.

temp_gg <-
  aligned_data_clean %>%
  ggplot(aes(x = Date, y = t_avg_C, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Temperature (C))", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Elwha. 

temp_gg_Dungeness <-
  aligned_data_clean %>%
  filter(site_name == "Dungeness River") %>%
  ggplot(aes(x = Date, y = t_avg_C, color = site_name)) +
  geom_line() + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Temperature (C))", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Elwha. 

temp_gg_Elwha <-
  aligned_data_clean %>%
  filter(site_name == "Elwha River") %>%
  ggplot(aes(x = Date, y = t_avg_C, color = site_name)) +
  geom_line(color = "limegreen") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Temperature (C))", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

#Plot of precipitation over the years for the Elwha. 
temp_gg_Skokomish <-
  aligned_data_clean %>%
  filter(site_name == "Skokomish River") %>%
  ggplot(aes(x = Date, y = t_avg_C, color = site_name)) +
  geom_line(color = "dodgerblue") + 
  theme_minimal() +
  facet_wrap(~ gsub("_", " ", site_name), nrow = 3, scales = "free") +
  labs( x = "Year", y = "Temperature (C))", color = "Gage") +
  theme(strip.text = element_text(size = 12), 
        axis.title.x = element_text(size = 14), 
        axis.title.y = element_text(size = 14), 
        legend.position = "none",)

```


```{r}
#Line graphs with updated data set. 

aligned_data_clean <- aligned_data %>%
mutate(site_name = recode(site_name,
"Elwha_River" = "Elwha River",
"Dungeness_River" = "Dungeness River",
"Skokomish_River" = "Skokomish River"))


#Returns over time of all three rivers on one graph.
aligned_data_clean %>%
ggplot(aes(x = waterYear, y = abundance_qty, color = site_name)) +
geom_line() +
geom_point() + 
theme_classic() +
labs(
title = "Salmon Abundance Over Time",
color = "Site",
y = "Salmon Return Abundance",
x = "Year") + 
theme(legend.position = "bottom")

#Returns in each seperate river on seperate graphs stacked together.
aligned_data_clean %>%
ggplot(aes(x = waterYear, y = abundance_qty, color = site_name)) +
geom_line() +
geom_point() +
theme_classic() +
labs(title = "Salmon Abundance Over Time",
color = "Site",
y = "Salmon Return Abundance",
x = "Year") +
facet_wrap(~ site_name, scales = "free_y", ncol = 1) + 
theme(legend.position = "bottom")

#Salmon returns over the years in the Skokomish
SkokomishReturns <-
aligned_data_clean %>%
 filter(site_name == "Skokomish River") %>%
 ggplot(aes(x = waterYear, y = abundance_qty, color = site_name)) +
 geom_line(color = "dodgerblue") +
 geom_point(color = "dodgerblue") +
 theme_classic() +
 labs(title = "Salmon Abundance Over Time",
 color = "Site",
 y = "Salmon Return Abundance",
 x = "Year") +
 theme(legend.position = "bottom")

#Salmon returns over the years in the Elwha
ElwhaReturns <-
aligned_data_clean %>%
 filter(site_name == "Elwha River") %>%
 ggplot(aes(x = waterYear, y = abundance_qty, color = site_name)) +
 geom_line(color = "limegreen") +
 geom_point(color = "limegreen") +
 theme_classic() +
 labs(title = "Salmon Abundance Over Time",
 color = "Site",
 y = "Salmon Return Abundance",
 x = "Year") +
 theme(legend.position = "bottom")

#Salmon returns over the years in the Dungeness
DungenessReturns <-
aligned_data_clean %>%
 filter(site_name == "Dungeness River") %>%
 ggplot(aes(x = waterYear, y = abundance_qty, color = site_name)) +
 geom_line() +
 geom_point() +
 theme_classic() +
 labs(title = "Salmon Abundance Over Time",
 color = "Site",
 y = "Salmon Return Abundance",
 x = "Year") +
 theme(legend.position = "bottom")

```


```{r}
#This code places the three above graphs side by side so that trends between the three rivers can easily be examined.

q_gg + prcp_gg + temp_gg

q_gg +prcp_gg 

prcp_gg + temp_gg

q_gg + temp_gg


SkokomishReturns / q_gg_Skokomish / prcp_gg_Skokomish / temp_gg_Skokomish 

DungenessReturns / q_gg_Dungeness / prcp_gg_Dungeness / temp_gg_Dungeness 

ElwhaReturns / q_gg_Elwha / prcp_gg_Elwha / temp_gg_Elwha

```


#DO WE NEED THIS CHUNK?
```{r}
# lead the data so climate matches with returns, and some basic stats

gage_fish_lead %>%
  group_by(site_name, ann) %>%
  reframe(abd_qty_lead = Mode(abundance_lead), Flow = mean(Flow))  %>%
  ggplot(aes(x = abd_qty_lead, y = Flow, color = site_name)) +
  geom_point() +
  geom_smooth(method = "lm")


fish_aov <- gage_fish_lead %>% 
  anova_test(abundance_lead ~ Flow*prcp_daily_mm*t_avg_C)



gage_fish_lead %>%
  ggplot(aes(x = abundance_lead, y = Flow, color = site_name)) +
  geom_line() + geom_point()

```

#DO WE NEED THIS CHUNK? CHECK THE CHUNK BELOW I THINK I DID ALL THE SAME WITH THE UPDATED DATA
```{r}

# Calculate the average and std dev of returns for each stream

abundance_stats <- 
  gage_fish_lead %>%
  ungroup() %>%
  summarise(return_avg = round(mean(abundance_lead, na.rm = T)), 
            return_sd = round(sd(abundance_lead, na.rm = T)), 
            .by = site_name)


# Creating a chart with annual return as a single point while still keeping annual data for everything   else

annual_salmon_returns <-
  gage_fish_lead %>%
  group_by(site_name, ann) %>%
  reframe(
    abundance_lead = Mode(abundance_lead, na.rm = TRUE), 
    ann = make_date(
      year = Mode(ann_lead), 
      month = 10, 
      day = 1),
    high_flow = max(Flow)
  ) %>% 
  left_join(abundance_stats, by = "site_name") %>%
  mutate(outlier = (abundance_lead > return_avg + return_sd | abundance_lead < return_avg - return_sd)) # checks if a given year is outside of 1 sd

DischargeandReturns <-
  gage_fish_lead %>%
  filter(Date_lead < "2023-10-01") %>%
  ggplot(aes(x = Date_lead, y = Flow, color = site_name)) +
  geom_line() +
  facet_wrap(facets = "site_name", 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_lead), 
             color = "black", 
             size = .75) 

```

#DO WE NEED THESE TWO CHUNKS? WHAT DO THEY DO? DO THEY GET USED AGAIN?
```{r}
#Above code using new data frame to make it more accurate

# Calculate the average and std dev of returns for each stream

abundance_stats_2 <- 
  aligned_data %>%
  ungroup() %>%
  summarise(return_avg = round(mean(abundance_qty, na.rm = T)), 
            return_sd = round(sd(abundance_qty, na.rm = T)), 
            .by = site_name)


# Creating a chart with annual return as a single point while still keeping annual data for everything   else

annual_salmon_returns_2 <-
  aligned_data %>%
  group_by(site_name, waterYear) %>%
  reframe(
    abundance_qty = Mode(abundance_qty, na.rm = TRUE), 
    ann = make_date(
      year = Mode(waterYear), 
      month = 10, 
      day = 1),
    high_flow = max(Flow)
  ) %>% 
  left_join(abundance_stats_2, by = "site_name") %>%
  mutate(outlier = (abundance_qty > return_avg + return_sd | abundance_qty < return_avg - return_sd)) # checks if a given year is outside of 1 sd
```


```{r}
#Creates a chart using the new data set with two y axis, legend, colors, etc. of Date vs Flow vs Salmon returns

DischargeandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = Flow, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Flow (cfs)", sec.axis = sec_axis(~ ., name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")

```


```{r}
#none of these work quite right yet. But they are trying to plot temperature against salmon returns

TemperatureandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = t_avg_C, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Average Temperature (C)", sec.axis = sec_axis(~ ., name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")


TemperatureandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = t_avg_C, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Average Temperature (C)", limits = c(-10,40), sec.axis = sec_axis(~ ., name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")


TemperatureandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = t_avg_C, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Average Temperature (C)", limits = c(-10, 40), sec.axis = sec_axis(~ . * 5, name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")
```

```{r}
#These show precipitation vs. fish data but also have some graphing errors.


PrecipitationeandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Daily Precipitation (mm)", sec.axis = sec_axis(~ ., name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")

PrecipitationeandReturns_2 <-
  aligned_data %>%
  filter(Date < "2023-10-01") %>%
  ggplot() +
  geom_line(aes(x = Date, y = prcp_daily_mm, color = site_name)) +
  facet_wrap(~ site_name, 
             nrow = 3, 
             scales = "free") +
  geom_point(data = annual_salmon_returns_2, 
             aes(x = as.POSIXct(ann), 
                 y = abundance_qty), 
             color = "black", 
             size = .75) + 
  scale_y_continuous(name = "Daily Precipitation (mm)", limits = c(0, 200), sec.axis = sec_axis(~ . * 5, name = "Salmon Return Abundance")) + 
  theme_minimal() +
  theme(
 axis.title.y.right = element_text(color = "black"),
 legend.position = "bottom", # Position legend at the bottom
 legend.title = element_text(face = "bold") # Bold legend title
 ) +
 labs(color = "Site Name")



```


```{r}
#This code creates some stats about our aligned data.

aligned_data_stats <-
 aligned_data %>%
 filter(waterYear != 1986) %>%
 mutate(site_name = case_when(
site_name == "Dungeness_River" ~ "Dungeness River",
 site_name == "Elwha_River" ~ "Elwha River",
 site_name == "Skokomish_River" ~ "Skokomish River",
 TRUE ~ site_name
 )) %>%
 group_by(site_name, waterYear) %>%
 reframe(
 abundance_qty = Mode(abundance_qty),
 Flow_median = median(Flow, na.rm = TRUE),
 Flow_max = max(Flow, na.rm = TRUE),
 Flow_min = min(Flow, na.rm = TRUE),
 t_ann_median = median(t_avg_C, na.rm = TRUE),
 t_ann_min = min(t_avg_C, na.rm = TRUE),
 t_ann_max = max(t_avg_C, na.rm = TRUE),
 prcp_median = median(prcp_daily_mm, na.rm = TRUE),
 prcp_max = max(prcp_daily_mm, na.rm = TRUE),
 prcp_min = min(prcp_daily_mm, na.rm = TRUE)
 )

```

```{r}
# Creating a variety of linear regression models to analyze our data and determine potential trends between salmon returns and climate variables. 

fish_mlr_max <-
  aligned_data_stats %>%
  lm(abundance_qty ~ Flow_max + t_ann_max + prcp_max, .)

lapply(fish_mlr_max, summary)

fish_mlr_min <-
  aligned_data_stats %>%
  lm(abundance_qty ~ Flow_min + t_ann_min + prcp_min, .)

lapply(fish_mlr_min, summary)

fish_mlr_wcs <-
  aligned_data_stats %>%
  filter(site_name != "Elwha_River") %>%
  group_map(~ lm(abundance_qty ~ Flow_min + t_ann_max, data = .))

lapply(fish_mlr_wcs, summary)

aligned_data_stats %>%
  ggplot(aes(x = prcp_max, y = abundance_qty, color = site_name)) +
  geom_point() +
  geom_smooth(method = "lm") +
  facet_wrap(facet = "site_name", nrow = 3, scale = "free") +
  labs(
 color = "site name",
 x = "Max Annual Precipitation (mm)",
 y = "Salmon Return Abundance"
 ) +
 theme_minimal() +
 theme(
 legend.position = "bottom"
 ) 

aligned_data_stats %>%
 ggplot(aes(x = Flow_max, y = abundance_qty, color = site_name)) +
 geom_point() +
 geom_smooth(method = "lm") +
 facet_wrap(facet = "site_name", nrow = 3, scale = "free") + 
 labs(
 color = "site name",
 x = "Max Annual Discharge (cfs)",
 y = "Salmon Return Abundance"
 ) +
 theme_minimal() +
 theme(
 legend.position = "bottom"
 ) 

aligned_data_stats %>%
  ggplot(aes(x = Flow_min, y = abundance_qty, color = site_name)) +
  geom_point() +
  geom_smooth(method = "lm") +
  facet_wrap(facet = "site_name", nrow = 3, scale = "free") + 
  labs(
 color = "site name",
 x = "Minimum Annual Discharge (cfs)",
 y = "Salmon Return Abundance"
 ) +
 theme_minimal() +
 theme(
 legend.position = "bottom"
 )


#!!!!I cant get this to work on my end!!!!! breaks at the 2nd chunk
summary(fish_mlr_wcs)

ggPredict(fish_mlr_wcs, interactive = T)

fish_mlr_dung_wcs <-
  aligned_data_stats %>%
  filter(site_name == "Dungeness_River") %>%
  lm(abundance_qty ~ Flow_min + t_ann_max, .) %>%
  summary()
```


#DO WE NEED THIS CHUNK???
```{r}
# preliminary example of data surrounding a high returns year

gage_fish_lead %>%
  filter(site_name == "Dungeness_River", Date_lead >= "2006-08-01" & Date_lead < "2007-06-01") %>%
  ggplot(aes(x = Date_lead, y = Flow)) +
  geom_line(color = "blue")
  
```

```{r}
# plot all of the different salmon returns 
annual_salmon_returns %>%
  ggplot(aes(x = ann, y = abundance_lead, color = site_name)) +
  geom_line() +
  facet_wrap(~ site_name, nrow = 3, scale = "free")


# Create a function and a loop that creates a ggplot object for when abundance is outside of sd
outlier_plotter <-
  function (f) {
    if (f$outlier == TRUE) {
      print("she works")
    }else {
      print("she dont work")
    }
  }


annual_salmon_returns %>%
  filter(site_name == "Dungeness_River") %>%
  ggplot(aes(x = ann, y = abundance_lead, color = site_name)) +
  geom_line() +
  geom_point() 

Dungeness_data <-
  gage_fish_lead %>%
  filter(site_name == "Dungeness_River")


# uses some temp columns to create filtered data. 
# filtered dates are based on arbitrarily generated Julian days, so changing the range first requires figuring out the corresponding Julian days.
#this one works but only on Dungeness
dungeness_julian <- 
  Dungeness_data %>%
  filter(ann_lead != 1986, ann_lead != 2027) %>%   #remove these rows bc theyre less than 365 days and that breaks things
  group_by(ann_lead) %>%
  mutate(fake_dates = seq(ymd('1970-01-01'),
                          ymd('1970-12-31'), 
                          by = '1 day'),
         fake_julian = julian(fake_dates, origin = as.Date("1970-01-01"))
  ) %>%
  filter(!(between(fake_julian, 151, 211))) %>%
  bind_rows(                                    #re-add the eariler filtered rows 
    (filter(
      Dungeness_data, year(Date_lead) < 1987)
    ),
    .) %>%
  bind_rows(
    (filter(
      Dungeness_data, year(Date_lead) > 2026)
    ),
    .) %>%
  arrange(Date_lead)


# attempting to do it to all sites
gage_fish_lead_filt <-
  gage_fish_lead %>%
  group_by(site_name) %>%
  filter(ann_lead != 1986, ann_lead != 2027) %>%   #remove these rows bc they're less than 365 days and that breaks things
  group_by(ann_lead) %>%
  mutate(fake_dates = seq(ymd('1970-01-01'),
                          ymd('1970-12-31'), 
                          by = '1 day'),
         fake_julian = julian(fake_dates, origin = as.Date("1970-01-01"))
  ) %>%
  filter(!(between(fake_julian, 151, 211))) %>%
  bind_rows(                                    #re-add the earlier filtered rows 
    (filter(
      gage_fish_lead, year(Date_lead) < 1987)
    ),
    .) %>%
  bind_rows(
    (filter(
      gage_fish_lead, year(Date_lead) > 2026)
    ),
    .) %>%
  arrange(Date_lead)


# create it as a function and lapply it?

migration_filter <-
  function(f) {
    f %>%
      filter(ann_lead != 1986, ann_lead != 2027) %>%   #remove these rows bc theyre less than 365 days and that breaks things
      group_by(ann_lead) %>%
      mutate(fake_dates = seq(ymd('1970-01-01'),
                              ymd('1970-12-31'), 
                              by = '1 day'),
             fake_julian = julian(fake_dates, origin = as.Date("1970-01-01"))
      ) %>%
      filter(!(between(fake_julian, 151, 211))) %>%
      bind_rows(                                    #re-add the eariler filtered rows 
        (filter(
          f, 
          year(Date_lead) < 1987)),
        .) %>%
      bind_rows(
        filter(
          f, 
          between(Date_lead, ymd("2027-01-01"), ymd("2027-05-31")) |
          ymd(Date_lead) > ymd("2027-07-31")),
        .) %>%
      arrange(Date_lead)
  }

gage_fish_lead %>%
  filter(site_name == "Skokomish_River") %>%
  migration_filter() %>%
  view()

gage_fish_lead %>%
  group_by(site_name) %>%
  group_map(~ migration_filter(.x))


where_fucked <-
  function(g) {
    g %>%
      group_by(site_no) %>%
      summarize(length = length(Date),
                sum_na = sum(is.na(Flow))
      )
  }

migration_filter(aligned_data)

piss <-
  readRDS("./NWIS_data.rdata") %>%
  renameNWISColumns() %>%
  view()

where_fucked(piss)

where_fucked(gage_fish_lead)

where_fucked(aligned_data)



```


```{r}

# create a df thats just dungeness align data for easy testing. Remove once function works on full df
dungeness_align <-
  aligned_data %>%
  filter(site_name == "Dungeness_River")


# create a migration filter that works with the aligned_data df
migration_filter_align <-
  function(f) {
    f %>%
      filter(waterYear != 1986) %>%   #remove these rows bc theyre less than 365 days and that breaks things
      group_by(waterYear) %>%
      mutate(fake_dates = seq(ymd('1970-01-01'),
                              ymd('1970-12-31'), 
                              by = '1 day'),
             fake_julian = julian(fake_dates, origin = as.Date("1970-01-01"))
      ) }
    
  #   %>%
  #     filter(!(between(fake_julian, 151, 211))) %>%
  #     bind_rows(                                    #re-add the eariler filtered rows 
  #       (filter(
  #         f, 
  #         year(Date_lead) < 1987)),
  #       .) %>%
  #     bind_rows(
  #       filter(
  #         f, 
  #         between(Date_lead, ymd("2027-01-01"), ymd("2027-05-31")) |
  #         ymd(Date_lead) > ymd("2027-07-31")),
  #       .) %>%
  #     arrange(Date_lead)
  # }
  # 

migration_filter_align(dungeness_align) %>%
  view()


dungeness_align %>%
  filter(waterYear == 2005) %>%
  view()



dungeness_align %>%
  group_by(waterYear) %>%
  nrow()

group_map(why,nrow, ...)


dungeness_align %>%
  filter(waterYear == 1992) %>%
  group_by(Date) %>%
  filter(n() > 1) %>%
  distinct(Date) %>%
  pull(Date) 



```


```{r}
midas_r()


```

